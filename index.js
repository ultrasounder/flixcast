const express = require("express");

const fs = require("fs"); //file system Node.js filesystem API

const app = express(); // creates an instance of express

const port = 3000;

// app.get("/", (req, res) => {
//   res.send("Hello, World");
// });

// app.listen(port, () => {
//   console.log(`Example app listening on port ${port}!`);
// });
